%% This is the application resource file (.app file) for the,
%% application.
{application, mmmbot_images,
  [{description, "MMMBot image handler"},
   {vsn, "0.1.0"},
   {modules, [mmmbot_images_app,
              mmmbot_images_sup]},
   {registered,[mmmbot_images_sup]},
   {applications, [kernel, stdlib, ibrowse, erlcloud]},
   {mod, {mmmbot_images_app,[]}},
   {start_phases, []}]}.
